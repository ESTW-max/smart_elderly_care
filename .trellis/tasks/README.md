# Trellis 任务目录

任务按 `MM-DD-name/` 建目录，每个任务通常包含 `task.json`、`prd.md`、`design.md` 和 `implement.md`。新任务优先使用官方命令创建：

```bash
python3 ./.trellis/scripts/task.py create "功能名称" --slug feature-name
```

本仓库的框架初始化记录见 `08-28-bootstrap-framework/`。
