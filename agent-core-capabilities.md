# Agent 通用核心能力模型

> 一份编码 Agent 的能力参考框架。
>
> **来源**：基于对 [openai/codex](https://github.com/openai/codex) 源码的实读分析抽象而成（基线 commit `0a12b855a0`，2026-08-30；100 个 crate / 154 万行 Rust）。结论已去除具体实现细节，适用于评估或设计任意编码 Agent。
>
> **整理日期**：2026-08-30

---

## 目录

- [分层模型](#分层模型)
- [一、骨架层](#一骨架层定义-agent-之所以是-agent)
  - [1. 编排循环](#1-编排循环)
  - [2. 工具调用](#2-工具调用)
  - [3. 执行环境](#3-执行环境)
- [二、能力层](#二能力层决定能承接多复杂的任务)
  - [4. 上下文管理](#4-上下文管理)
  - [5. 状态持久化](#5-状态持久化)
  - [6. 任务委派（多 Agent）](#6-任务委派多-agent)
- [三、约束层](#三约束层决定敢不敢放它自己跑)
  - [7. 沙箱隔离](#7-沙箱隔离)
  - [8. 授权审批](#8-授权审批)
  - [9. 网络与凭据](#9-网络与凭据)
- [四、支撑层](#四支撑层决定能走多远)
  - [10. 模型接入](#10-模型接入)
  - [11. 扩展机制](#11-扩展机制)
  - [12. 可观测性](#12-可观测性)
- [总体判断](#总体判断)
- [附录：能力成熟度自检表](#附录能力成熟度自检表)

---

## 分层模型

按「缺失后果」而非「技术相似度」分层：

```
┌─────────────────────────────────────────────────┐
│  支撑层   模型接入 · 扩展机制 · 可观测性          │  缺了 → 难演进
├─────────────────────────────────────────────────┤
│  约束层   沙箱隔离 · 授权审批 · 网络与凭据管控    │  缺了 → 不敢用
├─────────────────────────────────────────────────┤
│  能力层   上下文管理 · 状态持久化 · 任务委派      │  缺了 → 只能干小活
├─────────────────────────────────────────────────┤
│  骨架层   编排循环 · 工具调用 · 执行环境          │  缺了 → 不是 Agent
└─────────────────────────────────────────────────┘
```

| 层 | 作用 | 差异化程度 |
|---|---|---|
| 骨架层 | 定义 Agent 的基本形态 | 低——已高度同质化 |
| 能力层 | 决定任务复杂度上限 | **高**——上下文管理是分水岭 |
| 约束层 | 决定可否放手自动执行 | **最高**——需啃系统底层，难复制 |
| 支撑层 | 决定长期演进空间 | 中 |

---

## 一、骨架层（定义 Agent 之所以是 Agent）

### 1. 编排循环

**本质**：`采样 → 决策 → 行动 → 观测 → 再采样`，直到收敛。这是 Agent 与「一问一答 LLM」的分水岭。

#### 必须解决的四个问题

| 问题 | 通用解法 |
|---|---|
| **何时停** | 无工具调用 / 达成目标 / 预算耗尽 / 外部信号——四类终止条件缺一不可 |
| **能否中断** | 用户随时可打断，且已完成的工作不丢失 |
| **能否转向** | 运行中注入新指令（steering），而非等跑完再纠正 |
| **能否恢复** | 挂起后可从断点续跑，而非只能重来 |

#### 关键设计点：循环分层

成熟实现都会把循环切成多级：

```
任务 (task)   —— 一个完整目标，可跨多个回合
  └─ 回合 (turn)  —— 用户感知的一次交互
       └─ 步 (step)   —— 一次模型请求 + 工具执行
            └─ 流 (stream) —— 逐事件消费模型输出
```

分层带来两个收益：

- **设置快照可在回合内保持不可变**，避免运行中配置漂移，天然并发安全
- **diff / 计费 / 展示按用户感知粒度聚合**，而非按内部请求次数

#### 易被低估的一点：转向的时机

不是「有输入就立刻插进去」。至少两个位置必须**延后处理**：

1. **回合刚启动时** —— 保证本轮原始输入先被采样
2. **上下文压缩刚结束时** —— 让被中断的工具链先续完

这个细节做错，外部表现就是「我说话它不理」或「它把上一件事忘了」。

---

### 2. 工具调用

**本质**：把模型意图翻译成可执行动作，再把结果结构化地喂回去。

| 能力 | 要点 |
|---|---|
| **定义与暴露** | schema 化描述；需能**动态控制暴露面**（不同阶段给不同工具） |
| **路由分发** | 名称 → 处理器映射，含命名空间隔离（避免扩展工具撞名） |
| **并行执行** | 无依赖调用并发执行；取消信号需级联传播 |
| **结构化报错** | 失败原因要让模型**能自愈**——「被沙箱拒绝，因为试图写 `/etc`」而非「exit 1」 |
| **工具发现** | 工具超过数十个后全量塞进 prompt 是浪费，需按需检索 |

#### 前沿方向：代码化调用

与其让模型一次发一个 function call，不如让它**写一段代码在受控运行时里编排多次调用**——循环、条件判断、中间结果处理全在代码内完成，一次往返可顶过去十几轮。

这是目前最值得关注的范式变化，会显著改变工具设计的取舍（工具应更原子、更可组合）。

---

### 3. 执行环境

| 维度 | 弱实现 | 强实现 |
|---|---|---|
| **命令执行** | 一次性 `bash -c` | **持久会话** + stdin 续写 + 真 TTY |
| **文件修改** | 全文覆写 | **结构化补丁**（省 token、可审计、冲突可检测） |
| **环境抽象** | 仅本地 | 本地 / 远程 / 容器统一抽象，文件系统与进程均可远程化 |
| **环境继承** | 空白环境 | 快照用户 shell 环境（别名、函数、PATH）并复用 |
| **输出控制** | 按字节截断 | 按 **token** 截断，保留头尾 |

> **持久会话 vs 一次性执行，是执行层最大的能力分野。**
>
> 只有前者才能处理「启动服务 → 等待就绪 → 发起请求 → 查看日志」这类真实工作流，以及 REPL、调试器、`ssh` 等需要交互的场景。

---

## 二、能力层（决定能承接多复杂的任务）

### 4. 上下文管理

> **这是长任务的天花板，也是工程上最难的一块。**

| 子能力 | 要点 |
|---|---|
| **历史组装** | 按模型模态过滤（不支持图像的模型不能塞图） |
| **压缩 / 摘要** | 上下文满了要能续跑，而非报错退出 |
| **Token 预算** | 消息、工具输出、图像**分别设预算**，避免单个巨型输出挤爆窗口 |
| **输出截断** | 按 token 而非字节，保留头尾关键信息 |
| **自省** | 模型能查询「我还剩多少」，并主动决定压缩或开新窗口 |

#### 判断成熟度的标志

**压缩是否内联在主循环里。**

- ❌ 把压缩当**异常处理**：catch 到超限再补救
- ✅ 把压缩当**循环的正常分支**：预算不足则压缩后继续

后者还需额外处理：压缩前后的钩子、图像预算的单独核算、压缩模型自身的降级链、压缩后工具链的续跑。

---

### 5. 状态持久化

| 层次 | 内容 |
|---|---|
| **会话录像** | 完整事件流落盘，支持恢复、分叉、审计 |
| **读取效率** | 从尾部倒扫读最近记录，而非每次全文件解析 |
| **结构化状态** | 项目、目标、产物、队列、Agent 关系——用真数据库而非 JSON 堆 |
| **跨会话记忆** | 会话结束后仍然留存的知识 |
| **版本兼容** | 多版本客户端并存时，**旧版本要能打开新版本写过的库** |

---

### 6. 任务委派（多 Agent）

不是「能起子进程」就算。判断标准是这四条：

| 判据 | 弱 | 强 |
|---|---|---|
| **关系结构** | 树 | **图**——支持复杂协作与交叉引用 |
| **控制粒度** | 发任务 → 等结果 | **可中途打断并改方向** |
| **投递时机** | 粗暴插入 | 在消息边界或工具调用结束后送达 |
| **并行隔离** | 共享工作区 | **各自独立工作副本**（否则并行改文件必然冲突） |

---

## 三、约束层（决定敢不敢放它自己跑）

### 7. 沙箱隔离

#### 分级策略

典型四档，且**磁盘权限与网络权限必须独立控制**：

| 档位 | 磁盘 | 网络 |
|---|---|---|
| 只读 | 只读 | 默认关闭，可开 |
| 工作区可写 | 工作目录 + 显式声明的额外路径 | 默认关闭，可开 |
| 外部沙箱 | 全盘（已处于外部隔离环境内） | 跟随外部设置 |
| 完全放开 | 不限 | 不限 |

#### 三条硬要求

1. **原生实现，不依赖容器**
   各平台使用系统级隔离原语。依赖 Docker 意味着在没有 Docker 的机器上完全裸奔。

2. **违规可捕获并结构化回传**
   让模型知道「为什么被拦」，才能自行调整策略，而非反复撞墙。

3. **可降级重试**
   被拒后自动尝试升级沙箱策略，并借助缓存避免重复打扰用户。

---

### 8. 授权审批

**核心矛盾**：问太多则无法自动化，问太少则不安全。

#### 分层解法

```
① 规则引擎（确定性，快）
   白/黑名单、模式匹配；最好是可编程策略而非硬编码规则
        ↓ 命中「需判断」
② 模型审查器（语义，慢）
   独立受限会话评估具体动作，返回严格结构化结论
        ↓ 无法判定
③ 用户审批（兜底）
   细粒度分类，而非只有「问 / 不问」两档
```

#### 模型审查器的三条铁律

| 铁律 | 原因 |
|---|---|
| **继承父环境的网络与权限约束** | 否则审查器自身成为提权通道 |
| **超时 / 异常 / 格式错误一律 fail closed** | 任何不确定都必须默认拒绝 |
| **必须有风险分级** | 不同风险等级对应不同的自动化程度 |

---

### 9. 网络与凭据

比「域名白名单」要做得多：

| 能力 | 说明 |
|---|---|
| **代理层逐请求裁决** | 而非只在启动时设置环境变量了事 |
| **连接归因** | 知道每条连接由哪个进程发起 |
| **凭据代理** | 密钥留在代理侧注入，**不下发给被沙箱的进程** |
| **凭据存储** | 存系统钥匙串，不落明文 |

> **凭据代理这一条最容易被忽略，也最关键。**
>
> 只要密钥进了被 Agent 控制的进程环境，所有沙箱和审批都可以被绕过——模型只需读一下环境变量。

---

## 四、支撑层（决定能走多远）

### 10. 模型接入

- 多 provider 抽象
- 多 wire 协议（Responses / Chat Completions 等）
- 流式传输（SSE / WebSocket）
- 重试与指数退避
- **模型清单服务端下发，而非客户端硬编码**
- 降级链（主模型不可用时的回退路径）
- 支持本地模型

---

### 11. 扩展机制

| 机制 | 扩展的是什么 | 进阶要求 |
|---|---|---|
| **工具协议（MCP）** | 外部能力接入 | **双向**——既能当 client 也能当 server |
| **生命周期钩子** | 在循环固定点插入自定义逻辑 | 覆盖会话起止、工具前后、压缩前后、停止、子 Agent 起止 |
| **技能 / 提示扩展** | 领域知识与工作流 | **能声明依赖并自动安装** |
| **插件** | 打包分发以上三者 | 支持远程分发与版本管理 |

---

### 12. 可观测性

- 结构化事件流
- 分布式追踪
- **Token 分项计量**——输入 / 输出 / 缓存读 / 缓存写 / 推理，必须分开
- 审计日志
- 性能与延迟指标

> 没有这一层，成本和行为都是黑盒，规模化使用无从谈起。

---

## 总体判断

### 1. 骨架层同质化，差异在约束层

循环 + 工具调用几乎人人都能做对。真正拉开差距的是**沙箱、授权、网络管控**——这些要啃操作系统底层，投入周期长、抄不动，是唯一可持续的护城河。

### 2. 上下文管理是任务复杂度的唯一上限

其他能力决定 Agent「能做什么」，只有上下文管理决定它「能做多久」。

一个能连续工作 8 小时的 Agent 和一个 20 分钟就失忆的 Agent，差别几乎全部集中在这一块。

### 3. 安全必须是纵深的

单点防御（只有沙箱、或只有审批）都会被绕过。可用的模型是五层串联，且**每层默认拒绝**：

```
规则引擎 → 沙箱强制 → 网络裁决 → 模型复核 → 用户审批
```

---

## 附录：能力成熟度自检表

用于评估一个具体 Agent 实现的完成度。

### 骨架层

- [ ] 循环有明确的四类终止条件（无工具调用 / 目标达成 / 预算耗尽 / 外部信号）
- [ ] 支持运行中打断，且已完成工作不丢失
- [ ] 支持运行中注入新指令（steering）
- [ ] 转向输入的处理时机经过设计（回合启动、压缩之后需延后）
- [ ] 循环内部分层（任务 / 回合 / 步）
- [ ] 回合内设置为不可变快照
- [ ] 工具支持并行执行，取消信号级联传播
- [ ] 工具报错结构化，模型可据此自愈
- [ ] 工具暴露面可动态控制
- [ ] 命令执行支持持久会话与 stdin 续写
- [ ] 支持真 TTY（可操作 REPL / 调试器）
- [ ] 文件修改走结构化补丁而非全文覆写
- [ ] 输出按 token 截断并保留头尾

### 能力层

- [ ] 上下文压缩内联在主循环，而非异常处理路径
- [ ] 消息 / 工具输出 / 图像分别设置 token 预算
- [ ] 历史按模型模态过滤
- [ ] 模型可自查剩余上下文
- [ ] 会话完整落盘，支持恢复与分叉
- [ ] 最近记录可倒序快速读取
- [ ] 结构化状态使用数据库而非 JSON 文件
- [ ] 存储层支持旧版本客户端打开新版本数据
- [ ] 子 Agent 关系为图结构
- [ ] 子 Agent 可中途打断改向
- [ ] 并行子 Agent 有独立工作副本

### 约束层

- [ ] 沙箱分级，磁盘与网络权限独立控制
- [ ] 沙箱为系统原生实现，不依赖容器
- [ ] 沙箱违规结构化回传给模型
- [ ] 被拒后可自动升级策略重试，且不重复打扰用户
- [ ] 命令策略为可编程规则，而非硬编码
- [ ] 存在独立于主模型的语义审查层
- [ ] 审查器继承父环境的网络与权限约束
- [ ] 审查器超时 / 异常 / 格式错误一律拒绝
- [ ] 审批分类细粒度，而非只有「问 / 不问」
- [ ] 网络请求逐条裁决
- [ ] 连接可归因到进程
- [ ] 凭据由代理侧注入，不下发到沙箱进程
- [ ] 凭据存于系统钥匙串

### 支撑层

- [ ] 支持多 provider 与多 wire 协议
- [ ] 模型清单服务端下发
- [ ] 存在模型降级链
- [ ] 支持本地模型
- [ ] MCP 双向（既是 client 也是 server）
- [ ] 钩子覆盖循环全部关键节点
- [ ] 技能可声明依赖并自动安装
- [ ] Token 分项计量（含缓存读写与推理）
- [ ] 具备分布式追踪与审计日志
